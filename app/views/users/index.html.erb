<h1 class="title">Users</h1>
<%@users.each do |u| %>
    <div class="tile" >
        <div>
            <% u.artists.each do |ua| %>
                <%= link_to ua.name, ua %>
            <% end %>
        </div>
        <%= u.username  %>
    </div>
<% end %>